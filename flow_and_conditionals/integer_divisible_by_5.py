"""Create a function that prints True if an integer is evenly divisible by 5,
and False otherwise."""

def divisible_by_five(number):
    if (number % 5) == 0:
        print(True)
    else: print(False)

#enter a number for checking
divisible_by_five(number=)