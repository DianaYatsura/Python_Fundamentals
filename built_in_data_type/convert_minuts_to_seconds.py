'''Write a function that takes an integer minutes and converts
it to seconds.'''

def convert(minutes):
    if int(minutes)!= 0:
        return minutes * 60
    else: return 0

# input the minutes for checking
print(convert(minutes=))